apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-bridge.backend.fullname" . }}
  labels:
    {{- include "ai-bridge.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    {{- include "ai-bridge.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
